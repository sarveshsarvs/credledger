const HOST = "192.168.42.107";
const FRONTEND_PORT = 3000;
const BACKEND_PORT = 5000;

const frontendAddress = `http://${HOST}:${FRONTEND_PORT}`;
const backendAddress = `http://${HOST}:${BACKEND_PORT}`;

export { frontendAddress, backendAddress };
